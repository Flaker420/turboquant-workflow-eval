name: turboquant_safe_v6
enabled: true
comparison_label: safe_v6
adapter:
  import_path: qwen35_turboquant_workflow_study.adapters.local_transformers_patch:LocalTransformersPatchAdapter
settings:
  backend_label: local_transformers_patch
  scope: full_attention_only
  profile: safe_v6
  quant_mode: symmetric_per_token_fake_quant
  k_bits: 8
  v_bits: 6
notes:
  - Full-attention-only local patch.
  - Keep K conservative at 8 bits.
  - Tighten V first, since V looked much more extreme than K in preflight.
