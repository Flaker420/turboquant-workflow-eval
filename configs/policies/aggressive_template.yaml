name: turboquant_aggressive
enabled: false
comparison_label: aggressive
adapter:
  import_path: turboquant_workflow_eval.adapters.external_stub:ExternalCompressionAdapterStub
settings:
  backend_label: replace_with_real_backend
  scope: full_attention_only
  profile: aggressive
notes:
  - Replace the adapter import path with a real backend before enabling this policy.
  - Intended for a second-pass stress comparison after the safe policy is working.
