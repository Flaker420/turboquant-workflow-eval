name: turboquant_safe
enabled: true
comparison_label: safe
adapter:
  import_path: turboquant_workflow_eval.adapters.turboquant:TurboQuantAdapter
settings:
  bit_width: 4
  profile: safe
notes:
  - Conservative compression profile for first workflow comparison.
