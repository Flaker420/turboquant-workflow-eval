name: turboquant_safe
enabled: false
comparison_label: safe
adapter:
  import_path: turboquant_core.adapters.workflow_eval:TurboQuantAdapter
settings:
  bit_width: 4

settings:
  backend_label: replace_with_real_backend
  scope: full_attention_only
  profile: safe
notes:
  - Replace the adapter import path with a real backend before enabling this policy.
  - Intended for your first conservative workflow comparison.
